import React from 'react'

function ActionPrompt({nudge_prompt, nudges}) {
    console.log(nudges)
  return (
    <div>
        <button>

        </button>
        <button>

        </button>
    </div>
  )
}

export default ActionPrompt